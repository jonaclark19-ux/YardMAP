{
  "name": "tarter-yard-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "check": "for f in api/*.js api/_lib/*.js enhancements.js; do node --check \"$f\" || exit 1; done"
  },
  "dependencies": {
    "nodemailer": "^6.9.15",
    "xlsx": "^0.18.5"
  }
}
