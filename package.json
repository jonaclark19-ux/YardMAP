{
  "name": "tarter-yard-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "check": "node --check api/login.js && node --check api/state.js && node --check api/alerts.js && node --check enhancements.js"
  }
}
